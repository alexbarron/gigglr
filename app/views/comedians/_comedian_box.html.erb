<div class="col-md-6">
  <%= link_to(comedian) do %>
    <div class="box panel panel-default">
      <div class="panel-body row">
        <div class="col-md-6">
          <h2>
            <%= link_to comedian.name, comedian %><br />
            (<span id="fans-<%= comedian.id %>"><%= comedian.fan_count %></span> <%= 'Fan'.pluralize(comedian.fan_count) %>)
          </h2>

          <%= follow_unfollow(comedian) %>

          <h2>Upcoming Shows:</h2> 
          <% comedian.shows.first(3).each do |show| %>
            <p><%= link_to "#{show.short_date}", show %></p>
          <% end %>
              
        </div>
        <div class="col-md-6">
          <%= link_to image_tag(comedian.picture.url(:medium), width: "230"), comedian %>
        </div>
      </div>
    </div>
  <% end %>
</div>