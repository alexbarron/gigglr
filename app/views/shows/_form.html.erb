<%= simple_form_for @show do |f| %>
	<%= f.input :name %>
	<%= f.input :description %>
	<%= f.input :showtime %>
	<%= f.label :venue_id %>
  <%= f.collection_select(:venue_id, Venue.all.order("name ASC"), :id, :name) %>
	<%= f.submit 'Submit' %>
<% end %>